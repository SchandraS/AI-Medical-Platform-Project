# Copy to .env for local (non-Docker) runs. docker-compose.yml sets these
# directly as container environment variables instead of reading this file.

DATABASE_URL=postgresql+psycopg://manas:manas@localhost:5432/manas
JWT_SECRET=dev-secret-change-me-in-production
CORS_ORIGINS=["http://localhost:5173","http://localhost:8080"]
